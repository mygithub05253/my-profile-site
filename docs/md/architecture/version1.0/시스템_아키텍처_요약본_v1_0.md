# 시스템 아키텍처 요약본 v1.0

> 2026-07-04 (KST) · 신규 제정 · 전체본: `시스템_아키텍처_설계서_v1_0.md`

## 한 줄 요약
서버·DB 없는 **이벤트 드리븐 정적 생성 아키텍처** — 상태는 git(content-hub SSOT), 연산은 Actions·Vercel 빌드·로컬 스케줄러. E2E 100% 검증 완료.

## 구성 요약
- 4-레포: content-hub(허브) · velog-backup · my-profile-site · mygithub05253 + profile-admin(예정, 무DB stateless)
- 결합은 git clone + HTTP(Hook/dispatch/GraphQL)만 — 코드 공유 없음, 독립 실패
- 빌드: sync-content(clone) → velite(zod=L2 게이트) → next SSG(+OG)

## 시퀀스 4종 (전체본 §3)
① 발행 전파(Obsidian→velog→Vercel+README) ② 사이트 빌드 ③ README 즉시+일일 백스톱 ④ admin CRUD(sha 낙관적 잠금)

## 보안 핵심
Secrets 한정 저장 · fine-grained 최소 스코프 · 자동화는 auto-sync 브랜치 전용 · admin은 OAuth allowlist 1인 · 토큰 캘린더(velog 2026-08-02)

## 장애 내성
velog 파손→ready 유지+수동 폴백 · 빌드 실패→이전 배포 유지 · dispatch 403→일일 백스톱 · SSOT 사본 4곳(git 분산)

## ADR 11건
Vercel / velog 자동+폴백 / hub Public / Obsidian 우선 / Velite / B안 통합 / admin 무DB / C안 Signal / **레포 분리(D-4 확정)** / **토큰 npm 패키지(R-4)** / **정적 데이터 data/ 이관(D-1)**
