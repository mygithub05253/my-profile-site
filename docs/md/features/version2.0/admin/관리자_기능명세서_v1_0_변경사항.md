# 관리자 기능명세서 v1.0 변경사항

> 2026-07-04 (KST) · **신규 제정** — 통합 기능명세서 v2.0에서 관리자 도메인 분리

## 분리 배경

사용자 지시(2026-07-04): 문서를 사용자/관리자로 구분 + **사용자/관리자 레포 분리 의향 확인** → gc-dating-app 체계(`features/versionX/admin/`)로 분리하고 레포 분리를 ADR-9로 명문화.

## 변경 내역

| CN | 내용 |
|----|------|
| CN-1 | 통합본의 FR-A(자동화 7종)·FR-M(profile-admin 6종)을 관리자 문서로 분리 — 자동화는 관리자(운영자) 도구로 분류 |
| CN-2 | 검토보고서 F-5 반영: 저장 방식을 3안 비교로 재프레임, **PR+auto-merge 권장안** 신설 (D-2 대기) |
| CN-3 | 검토보고서 F-6 반영: FR-A12 재빌드 트리거 경로 = posts+projects, drafts 제외 (PR-A 요건) |
| CN-4 | 검토보고서 F-7 반영: FR-A14 역방향 diff 대상 posts 한정 |
| CN-5 | 관리자 운영 체크리스트(토큰 갱신·주간 PR·403 대응) 신설 |
| CN-6 | 리서치 R-1~R-3 연동표 신설 — 결과 수신 후 §16·§17 갱신 예정 |

## 2026-07-04 (후속): D 확정 + R-1~R-3 반영

| CN | 내용 |
|----|------|
| CN-7 | **D-2 확정 (b)**: 저장 방식 = 브랜치 커밋(Git Database API 원자) → PR → auto-merge(GraphQL). 사전 요건(allow_auto_merge·required status check·토큰 권한) 명시 |
| CN-8 | **R-2 반영**: FR-M16을 Auth.js v5 + 숫자 ID allowlist 3중 방어(signIn/jwt·session/authorized)로 확정. JWT maxAge 단축 정책 |
| CN-9 | **R-1 반영**: FR-M21 409 처리 = sha 재조회 + 지수 백오프 3회. Contents API 제약(100MB/실용 50MB·rate limit) 명문화 |
| CN-10 | **R-3 반영 + FR-M22 신설(P1)**: 이미지 = assets/ git 커밋 유지, 클라이언트 압축·WebP 후 원자 커밋, raw 핫링크 금지, R2/Cloudinary 마이그레이션 조건 |
| CN-11 | **D-1 (c) 반영 + FR-M23 신설(P3)**: data/*.yml 정적 데이터 폼 CRUD |

## 2026-07-05 (후속): FR-M9·FR-M10 신설 — 사용자 아이디어 2건

| CN | 내용 |
|----|------|
| CN-12 | **FR-M9 신설(P2)**: 등록 레포의 완성 신호(topics `portfolio-ready` 또는 README 마커) push → GitHub REST API 메타데이터 + 고정 템플릿 치환으로 velog 글감 초안 자동 생성 → content-hub `drafts/`에 PR. FR-M19(초안 관리)의 상류 공급 단계로 경계 분리. **레포 생성(create) 자체는 트리거로 채택하지 않음**(빈 글 방지) |
| CN-13 | **FR-M10 신설(P1)**: profile-admin에서 GitHub 레포 지정 시 REST API로 메타데이터 조회 → FR-M17 프로젝트 편집 폼에 규칙 기반 프리필(title·slug·github·repoVisibility·period·stack·summary). category·scope·featured 등 취향 판단 필드는 자동 추론하지 않고 관리자가 직접 선택. 저장은 기존 FR-M17 PR+auto-merge 흐름 그대로 재사용(검증 우회 없음) |
| CN-14 | **공통 제약 명문화**: 두 기능 모두 유료 LLM API 미사용 — GitHub REST API(무료) + 템플릿/규칙 기반 매핑(순수 코드)만으로 구현 가능하도록 설계 (사용자 요청: 학생 신분, 비용 제약) |

## 미결 (v1.1 후보)

- FR-M9 트리거 방식(topics 라벨 vs README 마커) 최종 택1 — 구현 착수 시 결정
- FR-M9·FR-M10 as-built 반영 (구현 착수 후)
