# v1.0 → v1.1 변경사항

> 2026-07-04 (KST) · Minor(+0.1) — 아키텍처 변경 없음, 구현 결과(as-built) 반영

## 신규
- **11장 Phase 1~3 구현 결과** 추가 (구현 이력·E2E 검증·발견 결함·운영 교훈)
- **12장 실행 계획 현황** (완료/Phase 4/잔여 항목 구분)
- **13장 프로필 사이트 전면 재설계 착수 선언** → UI 도메인 문서로 분리
- content-hub에 `deploy-site.yml` 신규 배선 (trigger-vercel + notify-profile-readme 2-잡 구조)

## 변경
- FR 표에 구현 상태 열 추가 — FR-01/02/03/05/07/08 완료, FR-04 기본 썸네일 완료, FR-06 잔여
- slug 문자 집합 완화: `[A-Za-z0-9가-힣.-]` (velog url_slug 대소문자·점 보존)
- Obsidian 블로그 폴더 `30_Blog` → **`70_Blog`** 정정 (기존 30_Projects 번호 충돌)
- velog-backup 자동화 브랜치 `auto-sync/velog-backup` → **`auto-sync-velog-backup`** (git ref 충돌)
- 1.4 리스크 표: 실제 발생한 권한 결함 2건을 리스크→실증 사례로 갱신

## 검증 (2026-07-04 E2E)
- OG 썸네일: 홈 `/opengraph-image` 및 글별 `/blog/{slug}/opengraph-image` → **200 image/png (1200×630)** 프로덕션 확인
- deploy-site: trigger-vercel 잡 성공 (Vercel 재빌드), notify-profile-readme 잡 403 → 장애 격리(NFR-03) 동작 확인
- repository_dispatch 수신: 로컬 인증 dispatch로 blog-post.yml 트리거 확인
- blog-post.yml: RSS 수집(20건)·마커 치환(5줄) 정상, push만 403 → 원인 격리 완료

## 수정 PR
- mygithub05253 PR #2: blog-post.yml에 `permissions: contents: write` 추가 (**병합 대기**)

## 잔여 → v1.2 이월 후보
1. CONTENT_DISPATCH_TOKEN 스코프 확장 (사용자)
2. content-hub Actions PR 생성 허용 설정 (사용자)
3. Phase 4 고도화 (알림·Notion 보조·토큰 만료 경고 자동화)
