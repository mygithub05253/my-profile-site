# 콘텐츠 스키마 명세서 v1.0 변경사항

> 2026-07-04 (KST) · **신규 제정** (이전 버전 없음)

## 배경

기존에는 스키마 정의가 `features/version1.1/자동화_생태계_아키텍처_설계서.md` 4장에 내장되어 있어, gc-dating-app 문서 체계(도메인 분리) 대비 다음 문제가 있었다.

1. posts 스키마(as-built)와 projects 스키마(신설 설계)가 서로 다른 문서(설계서 4장 vs 요구사항 정의서 6장)에 분산
2. 검증 코드(validate_frontmatter.py)·Velite 스키마와 문서 간 정합성을 대조할 단일 기준 부재
3. 상태 전이·slug 규칙이 여러 문서에 중복 서술

## 조치

| CN | 내용 | 출처 |
|----|------|------|
| CN-1 | ERD 도메인 신설, posts 스키마를 as-built 코드 기준으로 재작성 | validate_frontmatter.py · velite.config.ts 대조 |
| CN-2 | projects 컬렉션 스키마 정본화 (16필드 + CHECK 4종) | 요구사항 정의서 v1.0 6장 |
| CN-3 | 상태 전이 다이어그램·slug 규칙·검증 3계층 매트릭스 신설 | 설계서 10.2 + PR #4 이력 |
| CN-4 | 사이트 내장 정적 데이터(records/stacks/profile) 스키마 정의 | UI 명세서 v1.0 |

## 2026-07-04 (후속): 사용자 결정 D-1·D-3 반영

| CN | 내용 | 출처 |
|----|------|------|
| CN-5 | **D-1 확정 (b)+(c)**: records/stacks/profile을 사이트 `src/data/` → **content-hub `data/` YAML 이관** (§8 재작성, 컬렉션 4~6번 신설). admin CRUD 대상화(FR-M23, P3). 구현은 PR-D 선행 커밋 | 검토보고서 D-1 · 사용자 결정 2026-07-04 |
| CN-6 | **D-3 확정**: credible-stock-research **공개 전환** (§4.1 repoVisibility public) — 사용자가 레포 설정 변경 필요. repoVisibility 배지 로직은 unstructured-data-processing(private 유지) 때문에 존속 | 사용자 결정 2026-07-04 |

## 후속 (v1.1 후보)

- PR-A 병합 후 projects 검증 규칙 as-built 확정 반영
- `series` 필드 L1 검증 추가 여부 결정
- `data/*.yml` L1 검증(YAML 스키마) 추가
